<h3><%= @doctor.name %></h3><br>
<ul>
<% @doctor.appointments.each do |appointment|%>
    <li><%= appointment.appointment_datetime.strftime("%B %e, %Y at %k:%M")%>
    <%= link_to appointment.patient.name, patient_path(appointment)%></li>
    <%end%>
    </ul>
